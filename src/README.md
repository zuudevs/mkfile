# src

Folder `src/` berisi kode sumber aplikasi. File utama:

- `main.cpp` — titik masuk program.

Untuk mengembangkan:

1. Ikuti langkah di `docs/QUICKSTART.md` untuk build.
2. Edit `src/main.cpp` dan bangun ulang.
